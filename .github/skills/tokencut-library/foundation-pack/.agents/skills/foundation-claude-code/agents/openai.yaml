interface:
  display_name: "Everything Claude Code"
  short_description: "Repo-specific patterns and workflows for tokencut-library"
  default_prompt: "Use the tokencut-library repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
